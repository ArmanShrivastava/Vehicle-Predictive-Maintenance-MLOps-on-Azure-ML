Application (PyTorch Training / Triton Serving)
        │
        ▼
CUDA Runtime (libcuda.so) — GPU programming API
        │
        ▼
cuDNN — Deep learning primitives (conv, attention, etc.)
        │
        ▼
TensorRT — Optimizes trained models for inference
        │
        ▼
NVIDIA Container Toolkit — Makes GPU accessible inside containers
        │
        ▼
NVIDIA Device Plugin — Exposes GPU as K8s resource (nvidia.com/gpu)
        │
        ▼
NVIDIA GPU Operator — Installs and manages all of the above on K8s
        │
        ▼
DCGM (Data Center GPU Manager) — Monitoring, health checks, metrics
        │
        ▼
MIG (Multi-Instance GPU) — Partitions one A100 into multiple GPUs